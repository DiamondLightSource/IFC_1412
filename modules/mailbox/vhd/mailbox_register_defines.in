# Access to mailbox on I2C bus
:MAILBOX        RW
    # Message number to read or write
    .MSG_ADDR   2
    # Byte offset in message to read or write
    .BYTE_ADDR  5
    # Data to write on write, data returned on read
    .DATA       8
    # Set for write cycle, otherwise .DATA is read from mailbox
    .WRITE
    # Slot number as written to mailbox
    .SLOT       4
